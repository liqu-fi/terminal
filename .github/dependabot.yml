version: 2
updates:
  # Keep GitHub Actions up to date — bumps SHA-pinned actions (e.g.
  # pnpm/action-setup) and the version comment, and refreshes tagged actions.
  - package-ecosystem: github-actions
    directory: /
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
